# cECG 非接触电极改板设计清单

> **修订约定**: 已废弃的决定用~~删除线~~保留原文，不直接删除，方便追溯改动原因。

## 1. 设计决策

| 项目 | 决定 |
|------|------|
| 总接头(线缆↔主板) | ~~DP9 (D-Sub 9针，母座在主板/公头在线缆)~~ ~~GH 9P (SM09B-GHS-TB，卧贴带锁扣)~~ **主板分立3座: 2×5P + 1×2P，与电极板同款直通对应** |
| 电极板↔线缆接头 | JST GH 1.25mm (P/N电极板5P SM05B-GHS-TB，RLD电极板2P SM02B-GHS-TB) |
| 屏蔽方案 | 独立双屏蔽 (P/N各一路 driven-shield) |
| 电极模式切换 | 主板0Ω焊桥，电容/有源 vs 接触验证 |

---

## 2. 接头方案

### 2.1 电极板↔线缆: JST GH 1.25mm 卧贴

| 板子 | 接头 | 信号 |
|------|------|------|
| P/N电极小板(各一个) | 5P **SM05B-GHS-TB** (C189896) | VDDA, V_BIAS, GND, SIG, SHIELD |
| RLD电极板 | 2P **SM02B-GHS-TB** | RLD, GND |

~~建议JST SH 1.0mm~~ → 改选GH系列，理由：
- **必须卧贴(SMD)**：小板底层整层是电极，直插通孔焊盘会穿到底层挖坏电极铺铜——所有直插款(PH/XH/ZH等)出局
- GH带锁扣(SH无锁易脱)，贴身设备线缆被拽不掉
- GH是无人机界标准接头(Pixhawk同款)，淘宝预压成品线遍地且便宜
- 配套插头散件：胶壳GHR-05V-S / GHR-02V-S，端子SSHL-002T-P0.2（但建议直接买预压成品线，1.25mm端子手工压废品率高）

小板体积小，优先贴身端用接头而非直接焊线缆，方便迭代时插拔更换电极板。

### 2.2 ~~线缆↔主板: DP9 (D-Sub 9针)~~ 【已废弃，改用2.3节GH 9P。废弃原因：D-Sub优势(金属壳体/螺丝锁紧/耐插拔)面向工业面板，贴身原型用不上；且上5下4交错排布需要针位重映射，易错】

~~**重要: D-Sub物理上是上5下4两排交错，不是一排直线9针。** 針位必须按物理相邻关系重新映射，否则"SIG两侧夹SHIELD/GND隔离"的用意会落空。~~

```
 1   2   3   4   5      ← 上排(5针)          [已废弃]
   6   7   8   9        ← 下排(4针)
```

~~物理相邻路径: 1-6-2-7-3-8-4-9-5。DE-9针位映射: 1=VDDA, 2=SHIELD_P, 3=GND, 4=SHIELD_N, 5=GND2, 6=V_BIAS, 7=SIG_P, 8=SIG_N, 9=RLD。壳体接地: 整束外屏蔽接D-Sub金属壳体，壳体与GND2(pin5)单点星形连接。公母: 主板母座，线缆公头。~~

### 2.3 ~~线缆↔主板: GH 9P (SM09B-GHS-TB)~~ 【已废弃，改用2.4节分立3座。废弃原因：单接头需做Y型分叉线束(共享VDDA/V_BIAS/GND汇接焊点多)；分立座线缆退化为1:1直通线，制作最简单且P/N可互换】

~~单排直线排列，原始线性隔离排布直接成立，无需重映射：1=VDDA, 2=V_BIAS, 3=SHIELD_P, 4=SIG_P, 5=GND, 6=SIG_N, 7=SHIELD_N, 8=RLD, 9=GND2(整束外屏蔽端接)。~~

~~期间还讨论过12P方案(SM12B-GHS-TB, 5+5+2镜像排布，每线直通无汇接)，被分立3座方案替代——分立座免去12根端子插壳对号，BOM还与电极板统一。~~

### 2.4 线缆↔主板: 分立3座 (2×5P + 1×2P)

主板放 **SM05B-GHS-TB ×2**(丝印标注P/N) + **SM02B-GHS-TB ×1**(RLD)，针位与电极板端**完全相同**：

| 5P座×2 (P座/N座) | 2P座 (RLD) |
|---|---|
| 1 VDDA / 2 V_BIAS / 3 SHIELD / 4 SIG / 5 GND | 1 RLD / 2 GND |

优点：
- **线缆是1:1直通线**：双头预压线剪断→SIG/SHIELD段换同轴→其余三根对焊，无Y分叉
- **BOM统一两个料号**：主板与电极板同款座子，采购/焊接复用
- **模块化**：P/N/RLD各自独立插拔排查；P/N线缆完全相同可互换，插错仅导联反相(QRS倒立)无损坏，热缩管颜色区分
- SIG(4)仍夹在SHIELD(3)/GND(5)之间，隔离排布保持

注意事项：
- **整束外屏蔽端接**：无GND2针了。三根线体外合并套编织屏蔽套管时，编织层在主板端拧辫子焊到专门的GND焊盘/测试点(布局预留)；或第一版不套外屏蔽，driven-shield已保住SIG，实测有工频干扰再补
- **应力消除**：线缆在主板出口处扎线座/热熔胶固定，拉力不传到锁扣
- 三个座子并排板边、出线同向，座间距≥3mm(胶壳比针距宽)
- 主板端同样有几cm预压线散线段(无屏蔽缺口)，SIG为低阻信号，可接受

---

## 3. 电极端小板设计 (P路，N路镜像)

### 信号流
```
金属电容板(背覆Kapton/阻焊做介质)
  → 高阻节点
    → 100MΩ电阻到V_BIAS (偏置)
    → OPA391 单位增益跟随
      → 输出分两路:
        ├─ 经~100Ω串阻 → SIG_P (回主板)
        └─ 直接驱动 SHIELD_P (guard ring + 线缆内屏蔽)
```

### 关键要点
- **100MΩ偏置电阻 + guard ring 全部留在电极端小板上**
- 高阻节点绝不出小板 —— 有源电极的核心意义
- OPA391供电就近加100nF去耦 (参照原C26/C27)
- Guard ring环绕高阻节点走线，driven电位 = OPA391输出
- 电极小板尽量小、尽量靠近金属板，减少高阻节点走线暴露

### 电容板与小PCB的连接方式（不要用"焊pin+飞线"）
- **不推荐**: 小板上焊一个pin再用飞线接到电极板 —— 飞线完全暴露在guard ring之外，等于给全板最敏感的高阻节点接了根天线；两处焊点还带来助焊剂残留漏电风险（几十~几百MΩ级残留电阻会和100MΩ偏置电阻抢电流，破坏偏置精度、引入低频漂移）
- **推荐(最优)**: 电容板直接是小PCB最外层的一块铺铜/贴片金属，背面覆Kapton或加厚阻焊做介质层。板到100MΩ/OPA391全程走内部PCB线，被guard ring全程包裹，零飞线零额外焊点
- **推荐(次优)**: 若金属板需单独做(如不锈钢片更耐用)，直接贴焊在PCB大焊盘上(面接触，零长度过渡)，不经过飞线
- **若结构上必须留一小段距离**: 用短同轴线(屏蔽层接guard/OPA391输出而非GND)，长度压在1cm内，不要用裸线
- 高阻焊点焊接后必须酒精清洗助焊剂残留 + 三防漆(conformal coating)保护，这一步对100MΩ节点是必须项而非可选

### Guard ring 连接规则（重要，防止误接）
- **小板本地**: guard ring只接OPA391输出引脚（极短走线，同一网络）；若双层板走高阻线，顶/底层都做ring，用过孔缝合ring自身，不得碰GND
- **延伸到线缆**: guard ring在PCB边缘直接接到该路单芯屏蔽线的屏蔽层导体（即SHIELD_P/SHIELD_N），本质是同一张被驱动等电位罩换了载体
- **主板/DP9这端 (Pin3 SHIELD_P / Pin7 SHIELD_N)**: **悬空NC，绝不接主板GND或任何其他网络**。接GND会让driven-shield退化成普通接地屏蔽，前功尽弃；两端电位不一致还会在屏蔽导线里产生持续电流，增加OPA391负载。可留测试点调试，正常应看到与SIG_P/N几乎同幅同相的波形

### RLD电极端
- 不需要缓冲
- 主板RLDOUT → R22(100k) → ~~Pin9(DP9)~~ ~~Pin8(GH 9P)~~ 主板2P座Pin1 → RLD电极板
- **RLD板务必做大**，耦合越强共模抑制越好（之前小RLD是问题根源）

### 3.1 P路原理图定稿 (2026-07, N路镜像)

| 元件 | 值 | 连接 |
|------|-----|------|
| U6 | OPA391 | IN+ = `E_PLATE_P`，IN−与OUT相连(跟随器，网络`Guard Ring P`)，V−=GND，V+=VDDA |
| R18 | 100MΩ | V_BIAS ↔ `E_PLATE_P` (偏置) |
| R19 | 1kΩ | OUT ↔ SIG_P 串阻 (100Ω更常规，1k可用，与主板10k抗混叠串联影响可忽略) |
| C27 | 100nF | VDDA–GND 就近去耦 |

- 高阻网络命名为 `E_PLATE_P`（不要留EDA自动匿名网络名如`$3N17`，改动后会被重新编号）
- **IN+节点上绝无GND**——曾出现过IN+误接GND的错误，检查时重点核对
- 电极连接：`E_PLATE_P` 经**一个盖油过孔**通到底层电极铺铜，不放裸露测试点（裸TP焊盘=高阻节点漏电口）
- OUT→Guard Ring P之间可留0Ω位（防长线缆屏蔽振荡，见第5节），线缆<50cm可省

### 3.2 小板PCB落地要点 (两层板：底层电极、顶层元件)

- **底层整层留给电极铜箔，任何其他信号严禁打过孔到底层**——GND等信号过孔下去要么短路电极、要么在电极里挖隔离岛并把干扰源引到电极正下方。全板唯一通底层的过孔就是`E_PLATE_P`那一个
- 高阻节点"领土" = U6 IN+焊盘 + 走线 + 过孔 + R18高阻侧焊盘，guard ring把这整片围住
- **Guard ring画法(立创EDA)**：闭合走线会被"自动移除回路"功能删除。三个办法：①设置→PCB→布线里关掉"移除回路"；②**用铺铜/实心填充画ring(推荐)**，网络设`Guard Ring P`，面对象不触发回路移除且拦截面积更大；③留0.3mm小口的C形ring，缺口朝板边无铜方向
- Guard ring在R18处从电阻本体下方两焊盘间穿过：高阻侧焊盘围在圈内，V_BIAS侧留圈外
- R18尽量贴近U6，缩小高阻区，guard圈小而完整
- U6相邻引脚(IN+与GND，SC70间距0.95mm)之间guard插不进去，物理极限，围好封装外部即可
- 高阻过孔盖油(tented)；贴皮肤板建议高阻区加三防漆——汗液潮气下FR4表面电阻掉到10⁸~10⁹Ω，与100MΩ偏置同数量级
- **走线长短不免除guard**：漏电取决于相邻铜的电位差与间距，不取决于走线长度；1mm走线+焊盘+孔环的暴露铜照样要围

### 3.3 电极材质结论

- **电容式电极不在乎金属材质**：铜被阻焊介质盖住，无电化学界面，铜/银/金电学等价。电容电极板=普通铺铜+阻焊(不开窗)，阻焊层本身就是介质，零成本最优解
- PCB导体层只能是铜，"换成银"只能做表面镀层(沉银)；且**沉银≠Ag/AgCl**——低漂移靠的是银/氯化银氧化还原电对(非极化电极)，纯银面依然是极化电极，贴皮漂移与普通金属差别不大
- 若需开窗露铜(接触焊盘等)：立创EDA在底层阻焊层画实心填充=开窗(阻焊层负片逻辑)；开窗面下单选**沉金**(平整耐汗液)，默认喷锡不平易氧化
- 接触验证模式直接用一次性Ag/AgCl凝胶电极片接到主板焊盘，不在PCB上折腾镀层

---

## 4. 主板侧改动

### 4.1 引出到~~DP9~~ ~~GH 9P~~ 分立3座的信号
- VDDA、V_BIAS、GND 引到总接头 (原V_BIAS只在板内)

### 4.2 原缓冲级处理
- 原U4/U6 (OPA391) 缓冲级已移到电极端 → 主板上去掉
- 保留位置做接触/电容切换焊桥

### 4.3 信号进ADS前的0Ω二选一焊桥 (IN2P/IN2N各一组)

**模式A: 电容/有源模式**
```
~~总接头~~主板P/N座的SIG_P/SIG_N → 10kΩ(R26/R27) + 100pF抗混叠 → IN2P/IN2N
```

**模式B: 接触验证模式**
```
临时接触电极焊盘(Ag/AgCl)
  → 串联~0.1µF交流耦合
  → 几MΩ偏置到V_BIAS
  → 同一IN2P/IN2N
```

- 同一时刻只焊一侧
- RLD两模式共用

### 4.4 输入ESD保护
- 总接头的SIG_P/SIG_N进板处加低漏电钳位
- 参照RLD那颗BAV199，注意选低漏电型号
- SHIELD线不需要钳位

---

## 5. 稳定性注意点

OPA391直接驱动有电容的屏蔽/guard属于容性负载：
- OPA391数据手册标称可稳定驱动高达**1nF**容性负载
- 短引线(几十cm级)一般没问题
- 如果线拉得很长、屏蔽电容大 → 在OPA391输出到屏蔽之间串小电阻(几十~100Ω)隔离，防振荡
- **留个位子，先不焊**

---

## 6. 验证步骤顺序

1. **先焊接触验证模式** (模式B)
   - 贴标准Ag/AgCl电极片
   - 电极在身体上拉开跨过心轴
   - 验证链路+几何能出QRS

2. **确认后切回有源电极** (模式A)
   - 焊接0Ω到电容/有源侧
   - 专测电容前端

3. **对比两种模式波形**
   - 确认有源电极改善效果

---

## 7. 线缆建议

**不是"一根两芯共用屏蔽"，而是两根各自独立屏蔽的单芯线** —— 每路信号自己的屏蔽层就是它的driven-shield，P/N不能共用屏蔽（共用会导致其中一路guard电位失配，等于白做）。

线材构成：
```
P路: 单芯屏蔽线×1 — 芯线=SIG_P，屏蔽层=SHIELD_P (接OPA391_P输出)
N路: 单芯屏蔽线×1 — 芯线=SIG_N，屏蔽层=SHIELD_N (接OPA391_N输出)
公共线: 4根普通导线(不需独立屏蔽) — VDDA, V_BIAS, GND, RLD
外层: 整束再包一层公共屏蔽/编织层 → ~~接D-Sub金属壳体~~ ~~接GH 9P的Pin9(GND2)~~ 主板端焊到专门GND焊盘/测试点 (整体抗外部耦合，非二次guard；第一版可不套，实测有工频干扰再补)
```

- 现成线材可选"逐芯独立屏蔽多芯线"(如Belden 8723类构型：双芯各自屏蔽+公共屏蔽)
- 或自制：2根细同轴线(各自屏蔽) + 4根普通线，同走一根护套

### 7.1 线缆制作方案 (~~三段式~~ 1:1直通线，每路独立)

```
~~[电极板GH座] ⇦插⇨ [GH预压线] ─焊接驳─ [自制主线缆] ─焊接驳─ [GH预压线] ⇦插⇨ [主板GH 9P座]~~

P路(N路相同):
[电极板5P座] ⇦插⇨ [5P双头预压线剪断] ─SIG/SHIELD段换同轴，其余3根对焊─ [另一半] ⇦插⇨ [主板P座5P]

RLD路:
[RLD板2P座] ⇦插⇨ [2P双头预压线] ⇦插⇨ [主板RLD座2P]  (低阻驱动信号，整根直用不用剪)
```

**备料**：
- GH1.25预压成品线：5P双头×2(P/N，同向线序)、2P双头×1(RLD)
  - ~~9P单头~~ 不再需要，两端针位相同，双头直通
  - 淘宝认准带锁扣的GH/GHR壳，大量"1.25mm端子线"实为Molex PicoBlade无锁壳，能插但锁不住
  - **注意双头线"同向/反向"**：需1脚对1脚(同向)；反向线剪断重接时按颜色对号即可，不影响
- 细同轴线×2 (RG174或1.13mm)、编织屏蔽套管(可选)、热缩管

**P路中段改造映射(N路同理)**：

| 预压线(GH端) | 中段换成 |
|--------------|--------|
| Pin4 SIG那根 | 同轴线**芯线** |
| Pin3 SHIELD那根 | 同轴线**编织层** |
| Pin1/2/5 (VDDA/V_BIAS/GND) | 原线直接对焊接回 |

**头尾散线段无屏蔽的处理**（可接受，理由：SIG为低阻节点，几cm耦合电容仅零点几pF，干扰衰减百万倍量级；且P/N拾取对称为共模，被差分+RLD+数字陷波兜底）：
- 外层编织套管盖过接驳点、推到离GH胶壳仅1~2cm处（接地屏蔽罩低阻信号即可，不需driven）
- 散线段拧麻花缩小环路面积
- P/N散线段长度对称（拾取对称→共模→被差分抑制）
- 散线段贴身固定，不悬空、远离电源线/充电器

**工艺要点**：
- 接驳点是全线最脆弱处：每个焊点单独套小热缩管，接驳区整体再套大热缩管；接驳点靠近电极板端
- 编织层拧成小辫再焊，与芯线焊点错开防短路
- 做完先万用表逐针蜂鸣测通(GH端子↔GH端子对应关系、芯-屏无短路)再用
- P/N两路用不同颜色热缩管标记防插反(插反=极性反转，QRS倒立)

---

## 8. 主板调试口 (CN1, HX PZ2.54-2x5P TP)

**现状(2026-07-18)**：只用了4针 — 1=VDD、3=SWDIO、5=SWCLK、2=GND(经去耦电容)，其余6针(4/6/7/8/9/10)空置。**NRST未引出到CN1**。

**已有复位电路**（无需新增）：SW2(KH-6X6X5H按键) + R13(10k上拉) + C21(100nF) 组成"RESET"网络，已接到STM32 NRST(pin7)。PC0经"ADS_RESET"网络单独控制ADS1292R的PWDN#/RESET#，与MCU复位互不干扰。

**改动1 — 将NRST引到CN1**：
- **Why**: 固件把SWD复用引脚改作他用、或进入低功耗睡眠时，调试器无法正常握手连接；调试口上有NRST才能做connect-under-reset救回。
- 做法：把现成的"RESET"网络接到CN1空针（建议pin10）

**改动2 — 针位改按ARM标准2×5排布**（非强制，图方便）：

```
标准ARM Cortex 10-pin(2.54mm):        本项目CN1(改后):
1 VCC   2 SWDIO                       1 VDD    2 SWDIO
3 GND   4 SWCLK                       3 GND    4 SWCLK
5 GND   6 SWO                         5 GND    6 (NC)
7 NC    8 NC                          7 (NC)   8 (NC)
9 GND   10 NRST                       9 GND    10 RESET(NRST)
```

- **Why**: 目前1=VDD/3=SWDIO/5=SWCLK/2=GND与ARM标准错位（标准是2=SWDIO/4=SWCLK/3=GND）。项目目前调试口是杜邦线手动对插，不改也能用；但改成标准排布后可直接使用现成ARM 10-pin转接排线/转接板，不用逐根杜邦线对号
- 不强制，属于顺手优化

---

## 9. 未决项

| 项目 | 状态 |
|------|------|
| 电极端小板具体PCB尺寸 | 待定 |
| 接触模式偏置电阻具体值 | 待定 (几MΩ级) |
| 串联交流耦合电容值 | 待定 (~0.1µF) |
| 电容板介质厚度/材料 | 阻焊(不开窗)为基线方案，可试Kapton胶带加厚 |
| OPA391输出串阻(防振) | 留0Ω位，先焊0Ω；线缆<50cm可省 |
| R19串阻值 | 当前1kΩ，可改100Ω(更常规)，两者皆可 |
| STM32运行状态指示LED(心跳灯) | 待定，暂不加。方案：空闲GPIO+限流电阻，低电平点亮，1Hz心跳/5Hz故障闪烁复用同一颗LED，颜色与LED1/LED2(充电指示)区分 |
